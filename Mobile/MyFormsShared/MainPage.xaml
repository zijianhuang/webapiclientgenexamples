<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FonlowAndroid"
             x:Class="FonlowAndroid.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
    	<Label Text="Welcome to Xamarin.Forms! ABC 中文" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />

        <Label Text="{Binding Count}" HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />

        <ListView x:Name="HeroesView" ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
